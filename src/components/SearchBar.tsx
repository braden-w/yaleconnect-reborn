import { Input } from "@chakra-ui/react"
// Create a round search bar with a dropdown for options to filter with
export function SearchBar() {
  return <Input placeholder="Search Clubs..." variant="outline" />
}
